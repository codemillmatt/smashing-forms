<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="SmashingRecipe.RecipeDetailPage"
    Title="{Binding Name}">
    <ContentPage.Content>
        <FlexLayout
            Margin="10"
            Direction="Column"
            AlignItems="Center"
            JustifyContent="SpaceEvenly">

            <Label Text="{Binding Name} StringFormat='Recipe Name: {0}'}"  />

            <Image Source="matt.png" FlexLayout.Basis="20%" />

            <Label FontAttributes="Bold" Text="Ingredients" FontSize="Large"
                   HorizontalOptions="FillAndExpand" FlexLayout.AlignSelf="flex-start" />
            <Label Text="{Binding Ingredients}" />

            <Label FontAttributes="Bold" Text="Directions" FontSize="Large"
                   HorizontalOptions="FillAndExpand" FlexLayout.AlignSelf="flex-start" />
            <Label Text="{Binding Directions}" />

        </FlexLayout>
    </ContentPage.Content>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" Clicked="Edit_Clicked" />
    </ContentPage.ToolbarItems>
</ContentPage>
